# 多语言支持 (i18n) 实施计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为账本 App 添加多语言支持，默认支持中文和英文，语言设置保存在 localStorage

**Architecture:** 使用 react-i18next 实现多语言，通过 localStorage 持久化语言设置，启动时检测系统语言

**Tech Stack:** react-i18next, i18next

---

## 文件结构

```
frontend/src/
├── i18n/
│   ├── index.ts          # i18n 初始化配置
│   └── locales/
│       ├── zh.json       # 中文翻译
│       └── en.json       # 英文翻译
├── App.tsx              # 添加 i18n Provider
├── main.tsx             # 引入 i18n
└── pages/
    ├── LoginPage.tsx    # 使用翻译
    ├── HomePage.tsx     # 使用翻译
    ├── AddRecordPage.tsx
    ├── StatsPage.tsx
    ├── LedgerPage.tsx
    ├── CategoryPage.tsx
    ├── TagPage.tsx
    ├── SettingsPage.tsx # 添加语言切换
    ├── ExportPage.tsx
    └── BudgetPage.tsx
```

---

## Chunk 1: i18n 基础设置

### Task 1: 安装 i18n 依赖

**Files:**
- Modify: `frontend/package.json`

- [ ] **Step 1: 安装依赖**

```bash
cd frontend && npm install i18next react-i18next
```

- [ ] **Step 2: Commit**

```bash
git add frontend/package.json frontend/package-lock.json
git commit -m "chore: install i18next and react-i18next"
```

---

### Task 2: 创建 i18n 配置

**Files:**
- Create: `frontend/src/i18n/index.ts`
- Create: `frontend/src/i18n/locales/zh.json`
- Create: `frontend/src/i18n/locales/en.json`

- [ ] **Step 1: 创建 i18n 初始化配置**

```typescript
// frontend/src/i18n/index.ts
import i18n from 'i18next'
import { initReactI18next } from 'react-i18next'

const resources = {
  en: { translation: require('./locales/en.json') },
  zh: { translation: require('./locales/zh.json') }
}

// 获取保存的语言或检测系统语言
const getSavedLanguage = () => {
  const saved = localStorage.getItem('language')
  if (saved && ['en', 'zh'].includes(saved)) {
    return saved
  }
  // 检测系统语言
  const browserLang = navigator.language.toLowerCase()
  if (browserLang.startsWith('zh')) {
    return 'zh'
  }
  return 'en' // fallback
}

i18n
  .use(initReactI18next)
  .init({
    resources,
    lng: getSavedLanguage(),
    fallbackLng: 'en',
    interpolation: {
      escapeValue: false
    }
  })

export default i18n
```

- [ ] **Step 2: 创建中文翻译文件**

```json
// frontend/src/i18n/locales/zh.json
{
  "common": {
    "save": "保存",
    "cancel": "取消",
    "delete": "删除",
    "edit": "编辑",
    "confirm": "确认",
    "loading": "加载中..."
  },
  "auth": {
    "welcomeBack": "欢迎回来",
    "enterCredentials": "输入您的凭据以访问您的账户",
    "email": "邮箱",
    "password": "密码",
    "signIn": "登录",
    "signUp": "注册",
    "noAccount": "没有账号？"
  },
  "nav": {
    "home": "首页",
    "stats": "统计",
    "settings": "设置",
    "categories": "分类",
    "tags": "标签",
    "ledgers": "账本"
  },
  "home": {
    "monthlyExpense": "本月支出",
    "income": "收入",
    "balance": "结余",
    "recentRecords": "最近记录",
    "searchPlaceholder": "搜索记录...",
    "noRecords": "暂无记录",
    "addFirst": "点击 + 添加第一条记录"
  },
  "addRecord": {
    "title": "添加记录",
    "expense": "支出",
    "income": "收入",
    "amount": "金额",
    "date": "日期",
    "category": "分类",
    "note": "备注",
    "notePlaceholder": "添加备注...",
    "save": "保存记录"
  },
  "stats": {
    "title": "统计",
    "overview": "概览",
    "byCategory": "分类",
    "monthly": "月度",
    "yearly": "年度"
  },
  "category": {
    "title": "分类管理",
    "incomeCategory": "收入分类",
    "expenseCategory": "支出分类",
    "addCategory": "添加分类",
    "system": "系统"
  },
  "tag": {
    "title": "标签管理",
    "addTag": "添加标签"
  },
  "ledger": {
    "title": "账本管理",
    "addLedger": "添加账本",
    "default": "默认账本"
  },
  "settings": {
    "title": "设置",
    "appearance": "外观",
    "light": "浅色",
    "dark": "深色",
    "system": "跟随系统",
    "profile": "个人信息",
    "username": "用户名",
    "email": "邮箱",
    "nickname": "昵称",
    "nicknamePlaceholder": "设置昵称",
    "changePassword": "修改密码",
    "currentPassword": "当前密码",
    "currentPasswordPlaceholder": "输入当前密码",
    "newPassword": "新密码",
    "newPasswordPlaceholder": "输入新密码",
    "confirmPassword": "确认新密码",
    "confirmPasswordPlaceholder": "再次输入新密码",
    "budgetSettings": "预算设置",
    "exportData": "导出数据",
    "language": "语言"
  },
  "budget": {
    "title": "预算设置",
    "monthlyBudget": "月度预算",
    "budgetAmount": "预算金额",
    "saveBudget": "保存预算",
    "thisMonthExpense": "本月支出",
    "used": "已使用",
    "remaining": "剩余",
    "description": "设置月度预算可以帮助您更好地控制支出。当支出达到预算的 80% 时会提醒您。"
  },
  "export": {
    "title": "数据导出",
    "description": "导出说明",
    "exportDesc": "导出当前账本的所有记录数据，支持 JSON 和 CSV 格式。",
    "loadData": "加载数据",
    "exportJson": "导出 JSON",
    "exportCsv": "导出 CSV",
    "reloadData": "重新加载数据",
    "recordCount": "记录数",
    "warning": "导出的数据仅包含您当前的账本记录"
  }
}
```

- [ ] **Step 3: 创建英文翻译文件**

```json
// frontend/src/i18n/locales/en.json
{
  "common": {
    "save": "Save",
    "cancel": "Cancel",
    "delete": "Delete",
    "edit": "Edit",
    "confirm": "Confirm",
    "loading": "Loading..."
  },
  "auth": {
    "welcomeBack": "Welcome Back",
    "enterCredentials": "Enter your credentials to access your account",
    "email": "Email",
    "password": "Password",
    "signIn": "Sign In",
    "signUp": "Sign up",
    "noAccount": "Don't have an account?"
  },
  "nav": {
    "home": "Home",
    "stats": "Stats",
    "settings": "Settings",
    "categories": "Categories",
    "tags": "Tags",
    "ledgers": "Ledgers"
  },
  "home": {
    "monthlyExpense": "Monthly Expense",
    "income": "Income",
    "balance": "Balance",
    "recentRecords": "Recent Records",
    "searchPlaceholder": "Search records...",
    "noRecords": "No records yet",
    "addFirst": "Click + to add your first record"
  },
  "addRecord": {
    "title": "Add Record",
    "expense": "Expense",
    "income": "Income",
    "amount": "Amount",
    "date": "Date",
    "category": "Category",
    "note": "Note",
    "notePlaceholder": "Add note...",
    "save": "Save Record"
  },
  "stats": {
    "title": "Statistics",
    "overview": "Overview",
    "byCategory": "Category",
    "monthly": "Monthly",
    "yearly": "Yearly"
  },
  "category": {
    "title": "Category Management",
    "incomeCategory": "Income Categories",
    "expenseCategory": "Expense Categories",
    "addCategory": "Add Category",
    "system": "System"
  },
  "tag": {
    "title": "Tag Management",
    "addTag": "Add Tag"
  },
  "ledger": {
    "title": "Ledger Management",
    "addLedger": "Add Ledger",
    "default": "Default"
  },
  "settings": {
    "title": "Settings",
    "appearance": "Appearance",
    "light": "Light",
    "dark": "Dark",
    "system": "System",
    "profile": "Profile",
    "username": "Username",
    "email": "Email",
    "nickname": "Nickname",
    "nicknamePlaceholder": "Set nickname",
    "changePassword": "Change Password",
    "currentPassword": "Current Password",
    "currentPasswordPlaceholder": "Enter current password",
    "newPassword": "New Password",
    "newPasswordPlaceholder": "Enter new password",
    "confirmPassword": "Confirm Password",
    "confirmPasswordPlaceholder": "Enter new password again",
    "budgetSettings": "Budget Settings",
    "exportData": "Export Data",
    "language": "Language"
  },
  "budget": {
    "title": "Budget Settings",
    "monthlyBudget": "Monthly Budget",
    "budgetAmount": "Budget Amount",
    "saveBudget": "Save Budget",
    "thisMonthExpense": "This Month Expense",
    "used": "Used",
    "remaining": "Remaining",
    "description": "Setting a monthly budget helps you control spending. You'll be notified when you reach 80% of your budget."
  },
  "export": {
    "title": "Data Export",
    "description": "Export Instructions",
    "exportDesc": "Export all records from current ledger in JSON or CSV format.",
    "loadData": "Load Data",
    "exportJson": "Export JSON",
    "exportCsv": "Export CSV",
    "reloadData": "Reload Data",
    "recordCount": "Record Count",
    "warning": "Exported data only contains records from current ledger"
  }
}
```

- [ ] **Step 4: Commit**

```bash
git add frontend/src/i18n/
git commit -m "feat: add i18n configuration and translation files"
```

---

## Chunk 2: 集成 i18n 到 App

### Task 3: 在 main.tsx 和 App.tsx 中引入 i18n

**Files:**
- Modify: `frontend/src/main.tsx`
- Modify: `frontend/src/App.tsx`

- [ ] **Step 1: 修改 main.tsx 引入 i18n**

```typescript
// frontend/src/main.tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './i18n' // 添加这行
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

- [ ] **Step 2: 修改 App.tsx 添加 Suspense 包裹**

App.tsx 已经使用 Suspense，无需大改

- [ ] **Step 3: Commit**

```bash
git add frontend/src/main.tsx
git commit -m "feat: integrate i18n into main entry"
```

---

## Chunk 3: 翻译各页面文本

### Task 4: 翻译 LoginPage

**Files:**
- Modify: `frontend/src/pages/LoginPage.tsx`

- [ ] **Step 1: 添加 useTranslation hook 并替换文本**

```typescript
import { useTranslation } from 'react-i18next'

// 在组件内添加
const { t } = useTranslation()

// 替换文本
// "Welcome Back" → {t('auth.welcomeBack')}
// "Enter your credentials..." → {t('auth.enterCredentials')}
// "Email" → {t('auth.email')}
// "Password" → {t('auth.password')}
// "Sign In" → {t('auth.signIn')}
// "Sign up" → {t('auth.signUp')}
// "Don't have an account?" → {t('auth.noAccount')}
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/pages/LoginPage.tsx
git commit -m "feat: add i18n to LoginPage"
```

---

### Task 5: 翻译 HomePage

**Files:**
- Modify: `frontend/src/pages/HomePage.tsx`

- [ ] **Step 1: 添加翻译并替换关键文本**

```typescript
const { t } = useTranslation()

// 替换
// "本月支出" → {t('home.monthlyExpense')}
// "收入" → {t('home.income')}
// "结余" → {t('home.balance')}
// "最近记录" → {t('home.recentRecords')}
// "搜索记录..." → {t('home.searchPlaceholder')}
// "暂无记录" → {t('home.noRecords')}
// "点击 + 添加第一条记录" → {t('home.addFirst')}
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/pages/HomePage.tsx
git commit -m "feat: add i18n to HomePage"
```

---

### Task 6: 翻译其他页面

**Files:**
- Modify: `frontend/src/pages/AddRecordPage.tsx`
- Modify: `frontend/src/pages/StatsPage.tsx`
- Modify: `frontend/src/pages/LedgerPage.tsx`
- Modify: `frontend/src/pages/CategoryPage.tsx`
- Modify: `frontend/src/pages/TagPage.tsx`
- Modify: `frontend/src/pages/ExportPage.tsx`
- Modify: `frontend/src/pages/BudgetPage.tsx`

- [ ] **Step 1: 批量翻译这些页面**

每个页面添加 `const { t } = useTranslation()` 并替换关键文本

- [ ] **Step 2: Commit**

```bash
git add frontend/src/pages/
git commit - "feat: add i18n to remaining pages"
```

---

## Chunk 4: 设置页面语言切换

### Task 7: 在设置页面添加语言切换

**Files:**
- Modify: `frontend/src/pages/SettingsPage.tsx`

- [ ] **Step 1: 添加语言选择 UI**

```typescript
import { useTranslation } from 'react-i18next'

const { t, i18n } = useTranslation()

const handleLanguageChange = (lang: string) => {
  i18n.changeLanguage(lang)
  localStorage.setItem('language', lang)
}

// 在设置页面添加语言选择
<div className="flex gap-2">
  <button
    onClick={() => handleLanguageChange('zh')}
    className={i18n.language === 'zh' ? 'bg-primary text-white' : ''}
  >
    中文
  </button>
  <button
    onClick={() => handleLanguageChange('en')}
    className={i18n.language === 'en' ? 'bg-primary text-white' : ''}
  >
    English
  </button>
</div>
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/pages/SettingsPage.tsx
git commit - "feat: add language switcher to settings page"
```

---

## Chunk 5: 测试验证

### Task 8: 测试多语言功能

- [ ] **Step 1: 启动开发服务器**

```bash
cd frontend && npm run dev
```

- [ ] **Step 2: 验证默认语言**

打开浏览器，检查默认语言是否正确（中文环境显示中文）

- [ ] **Step 3: 验证语言切换**

在设置页面切换语言，验证所有文本立即更新

- [ ] **Step 4: 验证持久化**

刷新页面，验证语言设置保持

- [ ] **Step 5: 验证 fallback**

修改 localStorage 为非法值，刷新页面，验证 fallback 到英文

- [ ] **Step 6: Commit**

```bash
git add .
git commit - "test: verify i18n functionality"
```

---

## 实施顺序

1. **Chunk 1**: 安装依赖 → 创建 i18n 配置
2. **Chunk 2**: 集成到 App
3. **Chunk 3**: 翻译各页面（可并行）
4. **Chunk 4**: 添加语言切换 UI
5. **Chunk 5**: 测试验证

---

## 预期结果

- [ ] 默认语言跟随系统（zh/en）
- [ ] 非支持语言 fallback 为英文
- [ ] 设置页面可切换语言
- [ ] 语言设置持久化到 localStorage
- [ ] 切换语言后 UI 立即更新
