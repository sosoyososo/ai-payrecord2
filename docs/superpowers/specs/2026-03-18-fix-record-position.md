# 修复编辑记录后位置变化问题

**Created**: 2026-03-18
**Status**: PENDING

## 问题

编辑记录后，记录在列表中的位置发生变化。

## 原因

后端按 `date DESC, id DESC` 排序。编辑记录时修改日期会导致记录按新日期重新排序。

## 解决方案

编辑记录时保留原日期，只在用户明确修改日期时才更新。

## 实施

修改 `EditRecordPage.tsx`：
- 加载记录时保存原始日期
- 提交时检查日期是否被修改
- 如果日期未变化，不发送 date 字段（后端会忽略不更新的字段，保留原值）
